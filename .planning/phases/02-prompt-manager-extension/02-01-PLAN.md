---
phase: 02-prompt-manager-extension
plan: 01
type: execute
wave: 1
depends_on: ["01-01", "01-02"]
files_modified:
  - pkg/prompt/manager.go
  - pkg/prompt/manager_bootstrap.go
  - pkg/prompt/manager_store.go
  - pkg/prompt/manager_test.go
autonomous: true

must_haves:
  truths:
    - "PromptManager interface has GetPromptByID, GetPromptWithContext, SetPrompt, DeletePrompt, ListPrompts, RenderPrompt, GetStore methods"
    - "Built-in prompts (system, supervisor, compacter, subagent) load lazily from embedded FS on first access"
    - "GetPromptByID loads from store or bootstraps built-ins as version 1.0.0"
    - "Built-in prompts have IsBuiltin=true and cannot be deleted"
    - "Store access is thread-safe with sync.Once per built-in prompt"
  artifacts:
    - path: "pkg/prompt/manager.go"
      provides: "Extended PromptManager interface with new methods"
      exports: ["GetPromptByID", "GetPromptWithContext", "SetPrompt", "DeletePrompt", "ListPrompts", "RenderPrompt", "GetStore"]
      min_lines: 50
    - path: "pkg/prompt/manager_bootstrap.go"
      provides: "Lazy init for built-in prompts from embedded FS"
      contains: "sync.Once"
      min_lines: 80
    - path: "pkg/prompt/manager_store.go"
      provides: "PromptStore integration for prompt manager"
      contains: "PromptStore"
      min_lines: 60
    - path: "pkg/prompt/manager_test.go"
      provides: "Tests for lazy init pattern and built-in bootstrapping"
      contains: "TestLazyBuiltinBootstrap"
      min_lines: 40
  key_links:
    - from: "pkg/prompt/manager_bootstrap.go"
      to: "pkg/prompt/store/store.go"
      via: "PromptStore interface"
      pattern: "PromptStore\\s+\\*store\\.PromptStore"
    - from: "pkg/prompt/manager_bootstrap.go"
      to: "pkg/prompt/templates/*.md"
      via: "embed.FS reading"
      pattern: "promptTemplates\\.ReadFile|embed\\.FS"
---

<objective>
Extend PromptManager with ID-based prompt access, lazy built-in prompt bootstrapping from embedded FS, and PromptStore integration for persistent prompt management.

Purpose: Enable prompt versioning and lazy loading while maintaining backward compatibility with existing prompt methods.
Output: Extended PromptManager interface with store integration and lazy built-in initialization.
</objective>

<execution_context>
@/home/denkhaus/.claude/get-shit-done/workflows/execute-plan.md
@/home/denkhaus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-types-and-store-layer/01-01-SUMMARY.md
@.planning/phases/01-core-types-and-store-layer/01-02-SUMMARY.md

# Codebase context
@pkg/prompt/manager.go
@pkg/prompt/types.go
@pkg/prompt/store/store.go
@pkg/prompt/store/provider.go
@pkg/di/container.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PromptManager interface</name>
  <files>pkg/prompt/manager.go</files>
  <action>
    Extend the PromptManager interface with new methods while keeping existing methods for backward compatibility:

    Add to PromptManager interface:
    - GetPromptByID(ctx context.Context, id string) (*Prompt, error) - Retrieve prompt by ID from store or bootstrap built-in
    - GetPromptWithContext(ctx context.Context, id string, renderCtx *RenderContext) (string, error) - Get and render prompt with context
    - SetPrompt(ctx context.Context, id string, content string, name string) (*Prompt, error) - Save new prompt version
    - DeletePrompt(ctx context.Context, id string) error - Delete prompt (respects IsBuiltin flag)
    - ListPrompts(ctx context.Context, filter *ListFilter) ([]*Prompt, error) - List prompts with filter
    - RenderPrompt(ctx context.Context, p *Prompt, renderCtx *RenderContext) (string, error) - Render prompt template
    - GetStore() PromptStore - Access underlying store for advanced operations

    Keep existing methods:
    - GetCompacterPrompt(data any) (string, error)
    - GetSystemPrompt() (string, error)
    - GetSupervisorPrompt() (string, error)
    - GetSubagentPrompt(role, description string) (string, error)

    Import "context" package.
    Reference store.ListFilter type from pkg/prompt/store package.
  </action>
  <verify>
    grep -n "GetPromptByID\|GetPromptWithContext\|SetPrompt\|DeletePrompt\|ListPrompts\|RenderPrompt\|GetStore" pkg/prompt/manager.go
    go build ./pkg/prompt/...
  </verify>
  <done>
    Interface has 7 new methods + 4 existing methods
    All methods compile without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement lazy built-in prompt bootstrapping</name>
  <files>pkg/prompt/manager_bootstrap.go</files>
  <action>
    Create new file pkg/prompt/manager_bootstrap.go with lazy initialization pattern:

    Structure:
    - Add PromptStore field to promptManager struct
    - Add sync.Once fields for each built-in prompt (systemOnce, supervisorOnce, compacterOnce, subagentOnce)
    - Create bootstrapBuiltinPrompt(baseID string) (*Prompt, error) method:
      * Uses sync.Once to ensure single initialization
      * Reads from embedded promptTemplates FS
      * Creates Prompt with IsBuiltin=true, Version=1.0.0
      * Saves to store via SaveNewVersion
      * Returns loaded prompt
    - Map template filenames to IDs:
      * "system" -> templates/subagent_system_prompt.md
      * "supervisor" -> templates/supervisor_system_prompt.md
      * "compacter" -> templates/compacter_prompt.md
      * "subagent" -> templates/subagent_task_prompt.md

    Error handling:
    - Return wrapped errors if template reading fails
    - Return wrapped errors if store SaveNewVersion fails

    Thread safety:
    - Each built-in prompt has its own sync.Once
    - Bootstrap check happens before store load

    Dependencies:
    - Import "sync" for sync.Once
    - Import "context" for context.Context
    - Import "github.com/Masterminds/semver/v3" for semver.New()
    - Import prompt store package as promptstore "github.com/denkhaus/gollum/pkg/prompt/store"
  </action>
  <verify>
    grep -n "sync.Once\|bootstrapBuiltinPrompt\|IsBuiltin.*true" pkg/prompt/manager_bootstrap.go
    grep -n "ReadFile\|SaveNewVersion" pkg/prompt/manager_bootstrap.go
    go build ./pkg/prompt/...
  </verify>
  <done>
    Four sync.Once fields defined (systemOnce, supervisorOnce, compacterOnce, subagentOnce)
    bootstrapBuiltinPrompt reads from embedded FS and creates Prompt with IsBuiltin=true
    Template files map correctly to prompt IDs
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement store integration and ID-based retrieval</name>
  <files>pkg/prompt/manager_store.go</files>
  <action>
    Create new file pkg/prompt/manager_store.go with PromptStore integration:

    Constructor update:
    - Modify NewPromptManager to accept PromptStore from DI
    - Store PromptStore in promptManager field
    - Use do.Invoke or optional injection pattern to get PromptStore

    Implement new interface methods:
    - GetStore() PromptStore - Return underlying store
    - GetPromptByID(ctx, id):
      * First check if ID is a built-in constant (system, supervisor, compacter, subagent)
      * If built-in: call bootstrapBuiltinPrompt to lazy-load
      * Call store.Load(ctx, id)
      * Return nil, store.ErrPromptNotFound if not found
    - SetPrompt(ctx, id, content, name):
      * Call store.SaveNewVersion(ctx, id, content, name)
      * Return wrapped errors
    - DeletePrompt(ctx, id):
      * Load prompt to check IsBuiltin flag
      * If IsBuiltin==true: return store.ErrPromptIsBuiltin
      * Call store.Delete(ctx, id)
      * Return nil (not error) if not found
    - ListPrompts(ctx, filter):
      * Call store.List(ctx, filter)
      * Convert store.ListFilter if needed
    - GetPromptWithContext: Placeholder (implement in 02-02)
    - RenderPrompt: Placeholder (implement in 02-02)

    Update existing NewPromptManager signature in manager.go:
    - Change from do.Injector to do.Injector, promptstore.PromptStore
    - Store PromptStore in struct field

    DI integration:
    - Provider function NewPromptManagerProvider(ctx context.Context, injector do.Injector) (PromptManager, error)
    - Injects PromptStore from DI container
    - Calls NewPromptManager with injected store

    Dependency note:
    - Since PromptStore may not be in DI container yet, use optional injection pattern:
      store, _ := do.TryInvoke[promptstore.PromptStore](injector)
      If nil, create InMemory store as fallback
  </action>
  <verify>
    grep -n "GetPromptByID\|SetPrompt\|DeletePrompt\|ListPrompts" pkg/prompt/manager_store.go
    grep -n "store\.Load\|store\.SaveNewVersion\|store\.Delete\|store\.List" pkg/prompt/manager_store.go
    grep -n "ErrPromptIsBuiltin" pkg/prompt/manager_store.go
    go build ./pkg/prompt/...
  </verify>
  <done>
    GetPromptByID bootstraps built-ins before store load
    DeletePrompt checks IsBuiltin flag and returns ErrPromptIsBuiltin
    All methods delegate to PromptStore correctly
  </done>
</task>

<task type="auto">
  <name>Task 4: Add tests for lazy bootstrapping</name>
  <files>pkg/prompt/manager_test.go</files>
  <action>
    Create tests for lazy initialization and built-in bootstrapping:

    Test cases (table-driven):
    - TestLazyBuiltinBootstrap:
      * Create manager with InMemory store
      * Call GetPromptByID for "system"
      * Verify prompt was bootstrapped with IsBuiltin=true, Version=1.0.0
      * Verify template content was loaded
      * Verify prompt was saved to store
    - TestBuiltinPromptProtection:
      * Bootstrap built-in prompt
      * Call DeletePrompt on built-in ID
      * Verify ErrPromptIsBuiltin is returned
      * Verify prompt still exists in store
    - TestGetPromptByID_NotFound:
      * Call GetPromptByID with non-existent ID
      * Verify nil prompt is returned (not error)
    - TestConcurrentBootstrap:
      * Launch multiple goroutines calling GetPromptByID for same built-in
      * Verify bootstrap only runs once (sync.Once)
      * Verify no race conditions (go test -race)

    Mock usage:
    - Use centralized mocks from pkg/mocks/mock_store.go
    - Mock store.Load to verify bootstrap behavior
    - Mock store.SaveNewVersion to verify prompt is saved

    Test structure:
    - Use t.Run() for subtests
    - Use assert package for assertions
    - Clean up store between tests
  </action>
  <verify>
    go test -v ./pkg/prompt/...
    go test -race ./pkg/prompt/...
  </verify>
  <done>
    All tests pass
    No race conditions detected
    Lazy init verified (bootstrap runs once)
  </done>
</task>

</tasks>

<verification>
- [ ] PromptManager interface extended with 7 new methods
- [ ] Built-in prompts load lazily from embedded FS on first access
- [ ] sync.Once ensures single initialization per built-in prompt
- [ ] GetPromptByID returns bootstrapped built-ins with IsBuiltin=true
- [ ] DeletePrompt returns ErrPromptIsBuiltin for built-in prompts
- [ ] All tests pass including race detector
- [ ] Build passes with go build ./pkg/prompt/...
</verification>

<success_criteria>
Built-in prompts (system, supervisor, compacter, subagent) are loaded lazily from embedded FS on first GetPromptByID call, bootstrapped as version 1.0.0 with IsBuiltin=true, and protected from deletion.
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-manager-extension/02-01-SUMMARY.md`
</output>
