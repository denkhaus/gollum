---
phase: 03-prompt-optimizer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/prompt/optimizer/types.go
  - pkg/prompt/optimizer/templates.go
  - pkg/prompt/optimizer/optimizer.go
autonomous: true

must_haves:
  truths:
    - "Optimizer accepts trajectories (Gollem messages) and current prompt"
    - "Gradient strategy runs reflection loop with think/critique/recommend tools"
    - "Meta-prompt strategy combines reflection and update in single phase"
    - "Prompt memory strategy performs single-shot optimization"
    - "Optimizer returns new prompt version with incremented SemVer and change description"
  artifacts:
    - path: pkg/prompt/optimizer/types.go
      provides: "Core optimizer types (Trajectory, Feedback, OptimizerInput, OptimizerResult)"
      contains: "type Trajectory struct"
    - path: pkg/prompt/optimizer/templates.go
      provides: "Prompt templates for all three strategies"
      contains: "DEFAULT_GRADIENT_PROMPT"
    - path: pkg/prompt/optimizer/optimizer.go
      provides: "PromptOptimizer interface and factory"
      contains: "type PromptOptimizer interface"
  key_links:
    - from: pkg/prompt/optimizer/types.go
      to: github.com/m-mizutani/gollem
      via: "import"
      pattern: "github.com/m-mizutani/gollem"
    - from: pkg/prompt/optimizer/optimizer.go
      to: pkg/llm/provider.go
      via: "LLM client dependency injection"
      pattern: "gollem.LLMClient"
---

<objective>
Create the Prompt Optimizer core with three optimization strategies (gradient, metaprompt, prompt_memory) that analyze agent trajectories and generate improved prompts.

Purpose: Enable agents to learn from their interactions by analyzing conversation trajectories to identify prompt improvement opportunities and automatically refine system prompts.

Output: Optimizer package with types, templates, interface, and factory function for creating strategy-specific optimizers.
</objective>

<execution_context>
@/home/denkhaus/.claude/get-shit-done/workflows/execute-plan.md
@/home/denkhaus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-types-and-store-layer/01-01-SUMMARY.md
@.planning/phases/01-core-types-and-store-layer/01-02-SUMMARY.md
@.planning/phases/02-prompt-manager-extension/02-01-SUMMARY.md
@.planning/phases/02-prompt-manager-extension/02-02-SUMMARY.md
@.planning/phases/02-prompt-manager-extension/02-03-SUMMARY.md
@docs/prompt-optimizer/README.md
@docs/prompt-optimizer/02-langmem-optimizer-reference.md
@docs/prompt-optimizer/04-trajectory-structure.md
@pkg/llm/provider.go
@pkg/prompt/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core optimizer types</name>
  <files>pkg/prompt/optimizer/types.go</files>
  <action>
Create pkg/prompt/optimizer/types.go with:

1. Package declaration: package optimizer
2. Imports: github.com/m-mizutani/gollem, fmt, strings, time
3. Core types:
   - Trajectory struct with Messages ([]gollem.Message) and Feedback (interface{})
   - Feedback struct with Score (float64), Comment (string), FailureModes ([]string), Outcome (string)
   - EditFeedback struct with Revised (string) and Edits ([]TextEdit)
   - TextEdit struct with OldText, NewText, Reason (all string)
   - OptimizerInput struct with Prompt (string), Trajectories ([]*Trajectory), UpdateInstructions (string), Feedback (string)
   - OptimizerResult struct with NewPrompt (string), ChangeDescription (string), WarrantsAdjustment (bool)
   - OptimizerStrategy type (string) with constants StrategyGradient, StrategyMetaPrompt, StrategyPromptMemory
   - OptimizerConfig struct with Kind (OptimizerStrategy), MaxReflectionSteps (int, default 5), MinReflectionSteps (int, default 1), Provider (string, default "anthropic")
4. Helper methods:
   - (t *Trajectory) Validate() error - validates messages and feedback
   - (t *Trajectory) FormatForLLM() string - formats trajectory for LLM consumption
   - formatSessions(trajectories []*Trajectory) string - helper to format multiple trajectories

CRITICAL: Use gollem.Message for Trajectory.Messages (NOT a custom Message type). Follow the exact field names and types from docs/prompt-optimizer/04-trajectory-structure.md.
  </action>
  <verify>
go build ./pkg/prompt/optimizer/...
  </verify>
  <done>
Core optimizer types defined with Trajectory using gollem.Message, all feedback types supported, and validation/format helpers implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create prompt templates</name>
  <files>pkg/prompt/optimizer/templates.go</files>
  <action>
Create pkg/prompt/optimizer/templates.go with:

1. Package declaration: package optimizer
2. Prompt templates as constants (adapted from Python to Go template syntax):
   - DEFAULT_GRADIENT_PROMPT - for reflection phase (uses {prompt}, {trajectories}, {update_instructions} placeholders)
   - DEFAULT_GRADIENT_METAPROMPT - for applying recommendations (uses {current_prompt}, {hypotheses}, {recommendations} placeholders)
   - DEFAULT_METAPROMPT - combined reflection and update (uses {prompt}, {trajectories}, {update_instructions} placeholders)
   - DEFAULT_PROMPT_MEMORY - single-shot optimization (uses {current_prompt}, {trajectory}, {feedback}, {instructions} placeholders)
3. Tool definitions for gradient strategy (these are Gollem tool specs, NOT Go functions):
   - ThinkToolSpec: gollem.ToolSpec{Name: "think", Description: "A reflection tool for reasoning over complexities and hypothesizing fixes."}
   - CritiqueToolSpec: gollem.ToolSpec{Name: "critique", Description: "A critique tool for diagnosing flaws in reasoning."}
   - RecommendToolSpec: gollem.ToolSpec with parameters for warrants_adjustment (bool), hypotheses (string), full_recommendations (string)

Note: These are string templates with {placeholder} syntax. Do NOT use Go's text/template syntax yet - that will be handled by the strategy implementations when rendering.
  </action>
  <verify>
go build ./pkg/prompt/optimizer/...
  </verify>
  <done>
Prompt templates defined for all three strategies with proper placeholder syntax for runtime rendering.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create optimizer interface and factory</name>
  <files>pkg/prompt/optimizer/optimizer.go</files>
  <action>
Create pkg/prompt/optimizer/optimizer.go with:

1. Package declaration: package optimizer
2. Imports: context, github.com/m-mizutani/gollem
3. PromptOptimizer interface:
   - Optimize(ctx context.Context, input *OptimizerInput) (*OptimizerResult, error)
4. NewOptimizer function:
   - Signature: func NewOptimizer(client gollem.LLMClient, config *OptimizerConfig) (PromptOptimizer, error)
   - Validates config and sets defaults (MaxReflectionSteps: 5, MinReflectionSteps: 1)
   - Returns strategy-specific optimizer based on config.Kind:
     * StrategyGradient -> returns newGradientOptimizer(client, config)
     * StrategyMetaPrompt -> returns newMetaPromptOptimizer(client, config)
     * StrategyPromptMemory -> returns newPromptMemoryOptimizer(client, config)
   - Returns error for unknown strategy
5. Strategy constructors (stub implementations - will be completed in next plan):
   - newGradientOptimizer(...) -> *gradientOptimizer
   - newMetaPromptOptimizer(...) -> *metaPromptOptimizer
   - newPromptMemoryOptimizer(...) -> *promptMemoryOptimizer

Note: These stubs should compile but will have incomplete Optimize methods. The full strategy implementations are in Plan 02.
  </action>
  <verify>
go build ./pkg/prompt/optimizer/...
  </verify>
  <done>
PromptOptimizer interface defined with factory function that creates strategy-specific optimizer instances.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. go build ./pkg/prompt/optimizer/... should succeed
2. All types should be exportable (capitalized names)
3. Trajectory should use gollem.Message (not custom type)
4. All three strategy constants should be defined
5. OptimizerConfig should have defaults for reflection steps
</verification>

<success_criteria>
1. Core optimizer types compile without errors
2. Prompt templates are defined with proper placeholder syntax
3. PromptOptimizer interface and factory exist and compile
4. Factory returns stub optimizer instances for all three strategies
5. Files follow Go conventions (package naming, exports, documentation)
</success_criteria>

<output>
After completion, create `.planning/phases/03-prompt-optimizer/03-01-SUMMARY.md`
</output>
