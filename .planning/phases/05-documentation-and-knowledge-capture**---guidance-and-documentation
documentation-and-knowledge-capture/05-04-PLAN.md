---
phase: 05-documentation-and-knowledge-capture
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "User can reference Prompt Optimizer usage from CLAUDE.md"
    - "CLAUDE.md provides usage examples and configuration"
    - "Examples are specific to Gollum project (this is allowed in CLAUDE.md)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project-specific Prompt Optimizer usage documentation"
      min_lines: 20 (added)
  key_links:
    - from: "CLAUDE.md"
      to: "pkg/prompt/optimizer package"
      via: "Usage examples and configuration"
      pattern: "Prompt.*Optimizer|GOLLUM_OPTIMIZER|optimization"
---

<objective>
Update CLAUDE.md with Prompt Optimizer usage examples, configuration guidance, and integration patterns specific to the Gollum project.

Purpose: Provide project-specific documentation for using the Prompt Optimizer within Gollum.
Output: CLAUDE.md updated with optimizer section including usage examples, configuration, and integration patterns.
</objective>

<execution_context>
@/home/denkhaus/.claude/get-shit-done/workflows/execute-plan.md
@/home/denkhaus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference existing patterns
pkg/prompt/optimizer/types.go
pkg/prompt/optimizer/optimizer.go
pkg/config/service.go
pkg/di/container.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Prompt Optimizer section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add a new section "## Prompt Optimizer" to CLAUDE.md after the existing content (preserve existing Mock Generation, Project Structure, and Agent Tools sections).

New section must include:

### Configuration
```yaml
## Prompt Optimizer

The Prompt Optimizer automatically improves agent prompts using execution trajectory feedback.

### Configuration

Environment variables for optimizer configuration:

- `GOLLUM_OPTIMIZER_STRATEGY`: Optimization strategy (default: "gradient")
  - Options: "gradient", "meta-prompt", "prompt-memory"
- `GOLLUM_OPTIMIZER_PROVIDER`: LLM provider for optimization (default: "anthropic")
  - Options: "anthropic", "openai", "gemini"
- `GOLLUM_OPTIMIZER_MAX_REFLECTION`: Maximum reflection steps for gradient (default: "5")
- `GOLLUM_OPTIMIZER_MIN_REFLECTION`: Minimum reflection steps for gradient (default: "2")

Example:
\`\`\`bash
export GOLLUM_OPTIMIZER_STRATEGY=gradient
export GOLLUM_OPTIMIZER_PROVIDER=anthropic
export GOLLUM_OPTIMIZER_MAX_REFLECTION=5
\`\`\`

### Usage

#### DI Integration

The PromptOptimizer is automatically registered in the DI container:

\`\`\`go
import "github.com/denkhaus/gollum/pkg/di"

// In your DI setup
optimizer := do.MustInvoke[optimizer.PromptOptimizer](container)
\`\`\`

#### Optimizing a Prompt

\`\`\`go
import "github.com/denkhaus/gollum/pkg/prompt/optimizer"

// Create trajectory from execution
trajectory := &optimizer.Trajectory{
    Messages: agentMessages, // From agent session
    Feedback: "The agent struggled with multi-step reasoning",
}

// Prepare optimization input
input := optimizer.OptimizerInput{
    Trajectory: trajectory,
    CurrentPrompt: "You are a helpful assistant...",
    Config: optimizerConfig,
}

// Run optimizer
result, err := opt.Optimize(ctx, input)
if err != nil {
    log.Fatal(err)
}

// Result contains improved prompt
fmt.Printf("New version: %s\n", result.Version)
fmt.Printf("Changes: %s\n", result.ChangeDescription)
\`\`\`

#### Integration Helper

Use the OptimizeAndSave helper for end-to-end optimization:

\`\`\`go
import "github.com/denkhaus/gollum/pkg/prompt/optimizer/integration"

// Load, optimize, and save in one call
result, err := integration.OptimizeAndSave(
    ctx,
    optimizer,
    promptManager,
    "supervisor@1.0.0", // Current prompt ID
    trajectory,
)

if err != nil {
    log.Fatal(err)
}

// New version saved as supervisor@1.0.1
\`\`\`

### Optimization Strategies

#### Gradient Descent (Default)
Multi-phase reflection with think/critique/recommend tools. Best for comprehensive analysis.

#### Meta-Prompt
Single-phase combined reflection and update. Faster, less comprehensive.

#### Prompt Memory
Direct single-shot optimization. Fastest for minor adjustments.

### Best Practices

- Run optimization at session end, not during execution
- Use gradient strategy for critical agent prompts
- Collect feedback from actual user interactions when possible
- Monitor optimization results for quality degradation
- Keep prompt history for rollback if needed
```

Preserve all existing content in CLAUDE.md. Append new section at end.
  </action>
  <verify>
CLAUDE.md exists and contains:
- All existing sections (Mock Generation, Project Structure, Agent Tools)
- New "## Prompt Optimizer" section
- Configuration subsection with environment variables
- Usage subsection with code examples
- DI integration example
- Optimization helper example
- Strategies subsection explaining each approach
- Best Practices subsection
  </verify>
  <done>
CLAUDE.md updated with comprehensive Prompt Optimizer usage documentation including configuration, usage examples, and best practices.
  </done>
</task>

</tasks>

<verification>
- CLAUDE.md contains new "## Prompt Optimizer" section
- All existing content preserved (Mock Generation, Project Structure, Agent Tools)
- Configuration section lists all GOLLUM_OPTIMIZER_* environment variables
- Usage section includes code examples for DI integration and optimization
- Strategies section explains gradient, meta-prompt, and prompt-memory approaches
- Best Practices section provides practical guidance
- Examples are specific to Gollum (using github.com/denkhaus/gollum imports)
</verification>

<success_criteria>
Measurable completion:
- CLAUDE.md updated with Prompt Optimizer section
- Configuration documentation included
- Usage examples provided with Gollum-specific imports
- Strategies documented
- Best practices documented
- All existing content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-knowledge-capture**---guidance-and-documentation
documentation-and-knowledge-capture/05-04-SUMMARY.md`
</output>
