<?xml version="1.0" encoding="UTF-8"?>
<!--
Simple Flow Example
Demonstrates: Input/Output interface, Context, Computed fields, Agents, States, LLM steps
Note: Fragments removed - use sub-flows for reusable step groups
-->
<flow name="simple-flow" version="1.0">
    <description>
        Minimal flow demonstrating core concepts including reactive computed fields.
        The is_complex computed field automatically updates when context.complexity changes.
    </description>

    <input>
        <string name="target" required="true" />
    </input>

    <output>
        <int name="complexity" />
        <string name="recommendation" />
        <bool name="needs_refactor" />
    </output>

    <context>
        <int name="complexity" />
        <string name="recommendation" />
        <bool name="needs_refactor" />
    </context>

    <computed>
        <bool name="is_complex" eval="GT(context.complexity, 10)" />
    </computed>

    <agents>
        <agent name="analyzer" model="anthropic/claude-opus-4-6">
            <prompt>Code analyzer - concise and actionable</prompt>
            <temperature>0.0</temperature>
        </agent>
    </agents>

    <states>
        <state name="analyze" initial="true">
            <steps>
                <step type="llm" agent="analyzer">
                    <prompt><![CDATA[
                        Analyze ${target} and provide:
                        1. Complexity score (1-20) - use set_context_field("complexity", <score>)
                        2. Brief recommendation - use set_context_field("recommendation", "<text>")
                        3. needs_refactor flag - use set_context_field("needs_refactor", true/false)
                    ]]></prompt>
                    <tools>set_context_field</tools>
                    <timeout>60s</timeout>
                </step>
            </steps>

            <transitions>
                <transition to="done" />
            </transitions>
        </state>

        <state name="done" />
    </states>
</flow>
